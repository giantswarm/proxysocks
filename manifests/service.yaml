apiVersion: v1
kind: Service
metadata:
  name: gs-proxy
  namespace: default
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-internal: true
  labels:
    app: gs-proxy
spec:
  type: LoadBalancer
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app: gs-proxy
